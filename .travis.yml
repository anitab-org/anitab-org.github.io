language: node_js
node_js:
  - 8.15.0

before_deploy:
  - npm install
  - npm run build

deploy:
  provider: script
  github_token: $GITHUB_API_KEY
  script: npm install --silent && npm run deploy
  on:
    branch: travis-integration
