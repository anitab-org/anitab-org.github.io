language: node_js
node_js:
  - 8.15.0

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_API_KEY
  keep_history: true
  - echo -e "running deployment script" 
  - yarn deploy
  on:
    branch: travis-integration
